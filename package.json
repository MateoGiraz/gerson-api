{
  "name": "gerson-api",
  "version": "1.0.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MateoGiraz/gerson-api.git"
  },
  "author": "Gerson Team",
  "license": "MIT",
  "scripts": {
    "compile": "rm -rf dist/lib && tsc && tsc --build tsconfig.es5.json",
    "dev": "npm run compile && cd dist/test && node index.js"
  },
  "devDependencies": {
    "@types/node": "^20.1.6",
    "typescript": "^4.6.3"
  }
}
